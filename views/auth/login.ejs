<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/auth.css" />
    <link rel="stylesheet" href="/css/all.min.css" />
  </head>
  <body>
    <div class="header">
      <img src="/img/logo.png" alt="" width="50px" />
      <span class="desc">Security Information Management System</span>
    </div>
    <div class="auth-container">
      <% if(success && success.length > 0){%>
      <div class="alert alert-success text-center fadeIn animated" id="flash">
        <%= success %>
      </div>
      <%}%> <% if(error && error.length > 0){%>
      <div class="alert alert-danger text-center fadeIn animated" id="flash">
        <%= error %>
      </div>
      <%}%> <% if(warning && warning.length > 0){%>
      <div class="alert alert-warning text-center fadeIn animated" id="flash">
        <%= warning %>
      </div>
      <%}%>
      <div class="container">
        <form action="/auth/login" method="POST">
          <h2 class="text-center mt-4"><i class="fas fa-lock"></i> Login</h2>
          <p class="text-center">It's nice having you back!</p>
          <div class="row">
            <div class="col-md-12">
              <div class="input-group mb-3">
                <span class="input-group-text" id="basic-addon1"
                  ><i class="fas fa-at"></i
                ></span>
                <input
                  type="email"
                  name="email"
                  id="email"
                  value="melijah200@yahoo.com"
                  class="input form-control p-2"
                />
              </div>
            </div>
          </div>
          <div class="input-group mb-3">
            <span class="input-group-text" id="basic-addon1"
              ><i class="fas fa-lock"></i
            ></span>
            <input
              type="password"
              name="password"
              value="password"
              id="password"
              class="input form-control p-2"
            />
          </div>
          <button id="add-me">Log In <i class="fas fa-arrow-right"></i></button>
          <a href="/auth/forgot-password" class="text-center d-block"
            >forgot password?</a
          >
        </form>
      </div>
    </div>
    <style>
      #add-me {
        width: 100%;
        border: none;
        background-color: purple;
        color: #fff;
        border-radius: 2mm;
        padding: 10px;
      }

      #add-me:hover {
        background-color: #ce06ce;
      }
    </style>
  </body>
</html>
